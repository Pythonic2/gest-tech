[project]
name = "gest-tech"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Pythonic2", email = "igoormarinhosilva@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
gest-tech = "gest_tech:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
