version: '3.8'
services:
  gesttech:
    build: .
    volumes:
      - .:/app
    ports:
      - "8001:8001"
    environment:
      - PYTHONUNBUFFERED=1
    pull_policy: build

    networks:
      - pi_default

networks:
  pi_default:
    external: true
