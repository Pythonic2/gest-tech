version: '3.8'
services:
  gesttech:
    build: .
    volumes:
      - .:/code
    ports:
      - "8001:8000"
    environment:
      - PYTHONUNBUFFERED=1
    pull_policy: build

    networks:
      - pi_default

networks:
  pi_default:
    external: true
